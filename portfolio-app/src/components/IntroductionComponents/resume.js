import React from "react";
import resume from "./resume.png";

const Resume = props => (
  <div className = "resume">
    <img src = {resume}/>
    <h5>My Resume</h5>
  </div>
)

export default Resume;

import React from "react";

const philHeader = props => (
  <div>
    <h1 id = "philHeader">My Philosophy</h1>
    <h5 id = "philSubHeader">Everything starts with the ability to motivate myself...</h5>
  </div>
);

export default philHeader;
